import styles from './FavoritePlaces.module.scss'

interface FavoritePlacesProps {};

const FavoritePlaces = (props : FavoritePlacesProps) => {
	return (
	<div className={styles['favorite-places']}>
		
	</div>
)};

export default FavoritePlaces;